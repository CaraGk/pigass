{% extends 'PigassCoreBundle::layout.html.twig' %}

{% block title %}
    {{ parent() }} - {% if structure is not null %}{{ structure.name }} - Modification {% else %} Structure - Ajout {% endif %}
{% endblock %}

{% block headline %}
    {% if structure is not null %}{{ structure.name }} - Modification {% else %} Structure - Ajout {% endif %}
{% endblock %}

{% block content %}
    {{ form(form) }}
{% endblock %}
