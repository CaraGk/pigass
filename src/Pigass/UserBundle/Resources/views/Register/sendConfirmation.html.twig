{% extends 'PigassCoreBundle::layout.html.twig' %}

{% block headline %}Adhésion - Validation{% endblock %}

{% block action %}
    <li class="btn">
        <div class="progress">
            <div class="progress-bar progress-bar-primary progress-bar-striped" role="progressbar" aria-valuenow="60" aria-ValueMin = "0"  aria-ValueMax = "100" style="width: 50%;">
                Confirmation d'e-mail
            </div>
        </div>
    </li>
    <li class="btn btn-warning">
        <a href="{{ path('user_register_confirmation_send', {'email': email, 'slug': slug}) }}" title="Renvoyer l'email de confirmation">
            {{ icon('envelope') }} Renvoyer la confirmation
        </a>
    </li>
    <li class="btn btn-primary">
        <a href="{{ path('user_register_join', {'slug': slug}) }}" title="Finaliser l'adhésion">
            Continuer {{ icon('arrow-right') }}
        </a>
    </li>
{% endblock %}

{% block content %}
    <div class="panel panel-default">
        <div class="panel-body">
            <p>Un email de confirmation a été envoyé à l'adresse {{ email }}. Cliquez sur le lien présent dans l'email pour activer votre compte.</p>
            <p>En cas de non-réception du mail, <a href="{{ path('user_register_confirmation_send', {'email': email, 'slug': slug}) }}">cliquez ici pour renvoyer la confirmation</a>.</p>
            <p>Vous pouvez d'ores et déjà poursuivre votre adhésion en cliquant sur le lien « Continuer ».
        </div>
        <div class="panel-footer row">
            <div class="btn btn-primary pull-right">
                <a href="{{ path('user_register_join', {'slug': slug}) }}" title="Finaliser l'adhésion">
                    Continuer {{ icon('arrow-right') }}
                </a>
            </div>
        </div>
    </div>
{% endblock %}
