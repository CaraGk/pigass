{% extends 'layout.html.twig' %}

{% block title %}
    {{ parent() }} - {% if gateway is not null %}{{ gateway.gatewayName }} - Modification {% else %} Moyen de paiement - Ajout {% endif %}
{% endblock %}

{% block headline %}
    {% if gateway is not null %}{{ gateway.gatewayName }} - Modification {% else %} Moyen de paiement - Ajout {% endif %}
{% endblock %}

{% block action %}
{{ mopa_bootstrap_menu('parameter') }}
{% endblock %}

{% block content %}
<div class="card">
    <div class="card-body">
        {{ form(form) }}
    </div>
</div>
{% endblock %}
